## 1. Создание модуля выбора инструментов

- [x] 1.1 Создать модуль `src/instruments/__init__.py` (пустой) и `src/instruments/selector.py`
- [x] 1.2 Реализовать функцию `parse_instrument_input(raw: str)` — разбор строки формата `ТИКЕР ТИП` с поддержкой необязательного типа
- [x] 1.3 Реализовать функцию `request_instrument_type(ticker: str)` — запрос типа инструмента (share/future) при его отсутствии
- [x] 1.4 Реализовать основную функцию `select_instruments()` — интерактивный цикл ввода, валидации через `find_working_instrument`, повторного запроса при ошибке, возврат списка `(ticker, instrument_type)`
- [x] 1.5 Добавить обработку пустой строки и строки из пробелов как сигнала завершения ввода
- [x] 1.6 Добавить требование минимум одного валидного инструмента перед возвратом

## 2. Модификация `make_decision` для поддержки идентификатора инструмента

- [x] 2.1 Добавить параметр `instrument_label=""` в `make_decision()` в `src/signals/generator.py`
- [x] 2.2 Включить `[instrument_label]` в формат сообщения о покупке, продаже и удержании

## 3. Модификация основного цикла оркестрации

- [x] 3.1 Изменить `run_bot()` в `src/scheduler/runner.py` — принять параметр `instruments: list[tuple[str, str]]` вместо использования фиксированных `TICKER`/`INSTRUMENT_TYPE`
- [x] 3.2 Добавить внутренний цикл по списку инструментов с обработкой каждого: загрузка свечей, индикаторы, сигналы, решение, уведомление
- [x] 3.3 Добавить обработку пустого DataFrame для отдельного инструмента — вывод `Нет данных для {ticker}` и переход к следующему
- [x] 3.4 Передавать `instrument_label` в `make_decision()` и формировать сообщение с идентификатором инструмента

## 4. Обновление точки входа

- [x] 4.1 Изменить `run.py` — перед вызовом `run_bot()` вызвать `select_instruments()` и передать результат

## 5. Обновление конфигурации

- [x] 5.1 Оставить `TICKER` и `INSTRUMENT_TYPE` в `src/config.py` как значения по умолчанию для обратной совместимости
- [x] 5.2 Добавить комментарий в `src/config.py` о том, что интерактивный выбор取代 эти константы при обычном запуске

## 6. Проверка

- [x] 6.1 Проверить запуск `python run.py` — интерактивный ввод работает, инструменты валидируются
- [x] 6.2 Проверить сценарий с одним инструментом
- [x] 6.3 Проверить сценарий с несколькими инструментами (фьючерс + акция)
- [x] 6.4 Проверить сценарий с невалидным тикером — запрашивается замена
- [x] 6.5 Проверить, что сообщения уведомлений содержат идентификатор инструмента

## 7. Unit тесты

- [x] 7.1 Установить pytest
- [x] 7.2 Создать `tests/test_selector.py` — тесты `parse_instrument_input`, `request_instrument_type`, `select_instruments`
- [x] 7.3 Создать `tests/test_signals.py` — тесты `make_decision` с `instrument_label` и `get_last_signals`
- [x] 7.4 Создать `tests/test_runner.py` — тесты `run_bot` с multi-instrument loop
- [x] 7.5 Запустить все тесты — 47/47 пройдено
