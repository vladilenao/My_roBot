## 1. Конфигурация `[trading]`

- [x] 1.1 Добавить секцию `[trading]` в `default.toml`: `initial_deposit = 100000`, `max_risk_pct = 2.0`, `journal_file = "trade_journal.csv"`, `clearing_times = ["14:05", "19:00"]`
- [x] 1.2 В `src/config_loader.py`: валидация ключей секции `trading` (незнакомые ключи → `ConfigError`; `initial_deposit` — целое > 0; `max_risk_pct` — дробное в (0, 100]; `clearing_times` — список `HH:MM`; `journal_file` — строка)
- [x] 1.3 В `src/config.py`: экспорт констант `INITIAL_DEPOSIT`, `MAX_RISK_PCT`, `JOURNAL_FILE`, `CLEARING_TIMES` (импортируемых через `from src.config import ...`)
- [x] 1.4 Признак «торговый режим активен» (наличие секции `trading` после слияния конфигов) — функция в `src/config.py` для выбора реализации исполнения в `run.py`
- [x] 1.5 Unit-тесты `tests/unit/`: валидация секции `[trading]`, импорт констант, дефолты

## 2. Дневник сделок (`src/trade_journal/`)

- [x] 2.1 Модуль `TradeJournal`: разрешение пути файла через `app_dir()` и `JOURNAL_FILE` (для сборки — рядом с исполняемым файлом)
- [x] 2.2 Заголовок CSV — человекочитаемые русские названия колонок (`id,position_id,Статус,Время заявки,Дата,Сторона,Время входа,Депозит,Риск %,Риск руб,Цена входа,Цена стопа,Кол-во,Цена выхода,Прибыль руб,Комиссия руб,Шаг цены,Стоимость шага,ГО (покупка),ГО (продажа),Контракт,Причина,Заметки`); технические поля `id`/`position_id` остаются латиницей; создание файла при первом событии (UTF-8-SIG)
- [x] 2.3 Append-only запись события новой строкой с уникальным `id` и общим `position_id`; генерация `position_id` формата `{тикер}-{uuid}`
- [x] 2.4 Чтение и восстановление: `TradeJournal.load()` прогоняет строки → баланс, открытые позиции (средняя цена, qty, стоп/тейк, риск-снимок), активные заявки; нечитаемые строки пропускаются в лог
- [x] 2.5 Запись reasoning в событие заявки: `reason = "<стратегия> [<профиль>]"`, `notes` (текст из контекста решения); сохранение актуальными во всех событиях позиции
- [x] 2.6 Клиринговый снимок в журнал (баланс, реализованный P/L, число позиций, `max_risk_pct` сессии)
- [x] 2.7 Метаданные контракта в записи сделки: `price_step`, `step_cost`, `go_buy`, `go_sell` (для фьючерсов — из кэша Tinkoff; для остальных — пусто/применимо)
- [x] 2.8 Unit-тесты `tests/unit/`: заголовок, append, position_id, восстановление, клиринговый снимок, пропуск битых строк

## 3. Портфель (`src/portfolio/`)

- [x] 3.1 `Account`: старт с `INITIAL_DEPOSIT`; баланс = депозит + реализованный P/L + плавающий P/L открытых позиций (mark-to-market по последней закрытой цене)
- [x] 3.2 `PositionManager`: открытие/закрытие позиций, частичный выход (qty минус, средняя не меняется), пересчёт средней цены при доборе
- [x] 3.3 Агрегированный риск открытых позиций (сумма `risk_rub`); `risk_ok(новый_риск)` vs `MAX_RISK_PCT × equity`
- [x] 3.4 Проверка ГО-ёмкости: сумма занятого ГО + ГО новой позиции ≤ баланс (ГО из метаданных контракта; для инструментов без ГО — пропуск)
- [x] 3.5 Отметка «свершена с превышением риска» и учёт контр-заявки: возврат позиции в лимит, фиксация результата контр-операции
- [x] 3.6 Unit-тесты `tests/unit/`: баланс с плавающим P/L, средняя цена при доборе, частичный выход, лимит риска, ГО

## 4. Брокер (`src/broker/`)

- [x] 4.1 Абстрактный `BrokerPort` с четырьмя границами: `load_state`, `place_order`, `track_bar`, `cancel_order` (границы подмены на реальный API)
- [x] 4.2 `JournalBroker`: интерпретация `Decision` → события (`entry`/`scale_in` → лимитка по цене сигнального бара; `exit_reason` без `exit_contracts` → полный выход; с `exit_contracts` → частичный выход `N`)
- [x] 4.3 Статус-машина ордера `NEW → FILLED | CANCELLED | EXPIRED`; переходы фиксируются записями дневника с причиной
- [x] 4.4 Исполнение лимиток по закрытым свечам: BUY при `low ≤ limit` (по лимитной цене), SELL при `high ≥ limit`; без исполнения по незакрытым барам
- [x] 4.5 Защитные стоп/тейк как стоящие ордера из `stop_loss`/`take_profit` позиции; срабатывание по закрытым свечам
- [x] 4.6 TTL лимитки: отмена при обратном сигнале стратегии на инструмент; тайм-дедлайн по ТФ (≤30m → 1ч, 1h → 4ч, ≥4h → до ближайшего клиринга)
- [x] 4.7 Клиринг: снятие неисполненных заявок, перевыставление защитных стоп/тейков, открытые позиции не трогаются, снимок в дневник
- [x] 4.8 FIFO-обработка сигналов тика: проверка `risk_ok` и ГО перед каждой заявкой; снятие неисполненной при превышении лимита (`reason="risk_cap"`); over-risk-ветка — отметка + контр-заявка
- [x] 4.9 Unit-тесты `tests/unit/`: lifecycle статусов, исполнение лимитки, стоп/тейк, TTL, клиринг, FIFO/risk_cap/over-risk

## 5. Размер позиции (риск-слой)

- [x] 5.1 `PositionSizer` в `src/decision/`: `qty = floor(риск_бюджет / (|вход−стоп| × стоимость_шага / шаг_цены))`, минимум 1, при qty=0 вход отклоняется
- [x] 5.2 Интеграция с балансом (`Account.equity()`), метаданными контракта и `max_risk_pct`
- [x] 5.3 Обогащение решения финансового входа: `risk_pct` (снимок конфигурации), `risk_rub`, количество — для дневника
- [x] 5.4 Unit-тесты `tests/unit/`: формула размера, целочисленность, отказ при qty=0, риск-снимок

## 6. Исполнение, оркестратор, сборка

- [x] 6.1 Расширить `ExecutionPort.execute`: возвращает результат исполнения (id заявки + статус или признак «нет сделки»)
- [x] 6.2 Реализация `BrokerExecutionPort` (композиция portfolio → broker → trade_journal); `NotifyOnlyExecutionPort` сохраняется как режим без торговли
- [x] 6.3 В `trading_bot`: после фильтра и risk-менеджмента передача действий в `BrokerExecutionPort`; на тике с закрытой свечой — `track_bar` (исполнение лимиток, стопы/тейки, TTL); на пустом тике — пропуск
- [x] 6.4 В `trading_bot`: восстановление портфеля из дневника до входа в главный цикл
- [x] 6.5 В `trading_bot`: клиринг по `CLEARING_TIMES` (МСК с поправкой `BAR_TIME_TZ_OFFSET_HOURS`) — `apply_clearing()` брокера при пересечении времени
- [x] 6.6 В `run.py`: выбор `BrokerExecutionPort` при активной секции `[trading]`, иначе `NotifyOnlyExecutionPort`; сборка `portfolio`/`broker`/`TradeJournal`
- [x] 6.7 Метаданные контракта из реального Tinkoff API (спецификации + futures margin: шаг цены, стоимость шага, ГО куп/прод) с кэшированием по инструменту
- [x] 6.8 Тест интegration конвейера: старые конфиги без `[trading]` работают в режиме уведомлений (регрессия; покрыто в `tests/unit/test_trading_config.py`)

## 7. Уведомления о событиях сделок

- [x] 7.1 Расширить `DecisionFormatter` шаблонами: «заявка подана», «вход исполнен», «частичный/полный выход», «отмена по причине», «превышение риска + контр-заявка», «клиринговый снимок»
- [x] 7.2 Проводка событий брокера в нотификатор через `BrokerExecutionPort`/`execution`
- [x] 7.3 Unit-тесты форматирования новых шаблонов

## 8. Документация

- [x] 8.1 Обновить `ARCHITECTURE.md`: модули `trade_journal`/`portfolio`/`broker`, поток данных, клиринг, точки подмены на реальный API
- [x] 8.2 Обновить `CHANGELOG.md`: раздел новой версии с перечнем изменений
- [x] 8.3 Прогнать интеграционный сценарий: сигнал → заявка → исполнение → стоп/выход → клиринг → рестарт с восстановлением (реестрируется в `CHANGELOG`/README при необходимости)