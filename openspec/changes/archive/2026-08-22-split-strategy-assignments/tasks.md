## 1. Конфигурация

- [x] 1.1 В `src/config.py` удалить `STRATEGY_ASSIGNMENTS`; создать `SHARE_STRATEGIES: dict[str, list[StrategyName]] = {}` и `FUTURE_STRATEGIES: dict[str, list[StrategyName]] = {"NG": ["macd_rsi_stoch"]}` с комментариями о смысле ключей (точный тикер / базовый актив в верхнем регистре)
- [x] 1.2 Убедиться, что импорт `src.config` остаётся лёгким (только `src.strategies.names`, без реестра и пакетов стратегий)

## 2. Валидация

- [x] 2.1 В `src/strategies/__init__.py` добавить в `validate_assignments` необязательный параметр-метку источника; сообщение об ошибке называет словарь, содержащий неизвестное имя, и перечисляет доступные имена
- [x] 2.2 Обновить unit-тесты валидации (`tests/unit/test_registry.py`): проверка обоих словарей, метка источника в тексте ошибки, прежняя семантика fail-fast

## 3. Раннер

- [x] 3.1 В `src/scheduler/runner.py` заменить импорт `STRATEGY_ASSIGNMENTS` на `SHARE_STRATEGIES` и `FUTURE_STRATEGIES`
- [x] 3.2 Реализовать lookup по правилу диспетчеризации: `instrument_type == "future"` → `FUTURE_STRATEGIES.get(ticker[:2].upper())`, иначе → `SHARE_STRATEGIES.get(ticker)`; пропуск без непустой привязки сохранить как есть (лог + continue до запроса данных)
- [x] 3.3 Вызвать `validate_assignments` для обоих словарей в начале `run_bot()` до цикла (с метками источников)

## 4. Тесты раннера

- [x] 4.1 Перевести фикстуры `tests/unit/test_runner.py` на новые словари: фьючерсные кейсы — инструмент типа `future` с контрактным тикером + запись базы в `FUTURE_STRATEGIES`, акционные кейсы — точный тикер в `SHARE_STRATEGIES`
- [x] 4.2 Добавить/обновить кейсы: стратегии фьючерса определяются базой независимо от суффикса контракта; нормализация регистра базы (`SiZ6` → запись `"SI"`); точный тикер акции не ищется в `FUTURE_STRATEGIES`
- [x] 4.3 Обновить кейсы fallback `run_bot()` без аргументов и пропуска инструмента без привязки (запрос данных не выполняется, лог о пропуске)
- [x] 4.4 Обновить кейс fail-fast: неизвестное имя хотя бы в одном из двух словарей останавливает запуск до первой итерации

## 5. Проверка

- [x] 5.1 Прогнать полный `pytest`; убедиться, что snapshot-тесты стратегий не затронуты
- [ ] 5.2 Ручная проверка: `python run.py`, выбрать контракт NG → сигналы приходят с прежним поведением уведомлений
