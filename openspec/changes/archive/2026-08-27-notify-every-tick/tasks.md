## 1. Оркестратор: уведомление о каждом тике

- [x] 1.1 В `src/bot/trading_bot.py` убрать фронтовую дедупликацию в `_emit`: безусловно вызывать `self._execution.execute(decision, instrument)` на каждом тике для каждой пары «инструмент × стратегия».
- [x] 1.2 Удалить поле `self._prev_state` и его инициализацию в `__init__` оркестратора (мёртвый код после 1.1).
- [x] 1.3 Обновить docstring класса `TradingBot`, где упоминается уведомление «фронтом сигнала», чтобы отразить новое поведение (уведомление о каждом тике).

## 2. Тесты

- [x] 2.1 Переписать `tests/unit/bot/test_trading_bot.py::test_dedup_same_signal`: ожидать, что при двух тиках с одинаковым сигналом (BUY, та же цена) решение доставляется оба раза (нет подавления).
- [x] 2.2 Переписать/добавить тест на дедупликацию по цене: при неизменном типе сигнала (например, два HOLD) с изменившейся ценой решение доставляется на каждом тике — критерий «изменение цены» не влияет на доставку.
- [x] 2.3 Обновить/адаптировать `test_dedup_emits_on_change` под новое поведение (либо переименовать, либо убрать, если логика фронта удаляется).
- [x] 2.4 Прогнать юнит-тесты (`pytest tests/unit`) и убедиться, что все проходят.
