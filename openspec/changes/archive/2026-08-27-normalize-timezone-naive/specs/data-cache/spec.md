## MODIFIED Requirements

### Requirement: Признак готовности свечи
Кэш ДОЛЖЕН определять готовность свечи по границе таймфрейма, вычисляемой планировщиком времени. Границы готовности и отслеживание загруженных баров ДОЛЖНЫ вестись в одном (tz-naive) виде времени без пояса, согласованно с планировщиком и загрузчиком.

#### Scenario: Готовность по границе
- **WHEN** момент текущего времени достиг границы закрытия свечи
- **THEN** соответствующий бар считается готовым и доступным стратегиям

#### Scenario: Единый naive-вид границ
- **WHEN** кэш сравнивает/хранит границы готовности и известные бары
- **THEN** все значения времени приводятся к единому tz-naive состоянию (без пояса), без смешения naive и aware
