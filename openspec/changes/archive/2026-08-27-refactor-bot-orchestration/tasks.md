# Tasks: Рефакторинг оркестрации робота (TradingBot)

## 1. Модель инструмента

- [x] 1.1 Создать `src/instruments/model.py` с dataclass `Instrument` (поля `label`, `ticker`, `instrument_type`) и функцией нормализации 2-/3-кортежей в `Instrument`
- [x] 1.2 Добавить unit-тесты `tests/unit/instruments/test_model.py`: 3-кортеж, 2-кортеж (fallback label), неизвестный размер кортежа

## 2. Планировщик времени

- [x] 2.1 Создать `src/scheduler/timing.py` с классом `CandleScheduler` (инжектируемый `clock`, словарь периодов для `1m/5m/15m/1h/1d/1w/1M`)
- [x] 2.2 Реализовать `next_candle_close()` и `_floor()`/`_period()` для расчёта границ по календарной сетке (UTC), включая неделю и месяц
- [x] 2.3 Реализовать `wait_until_candle_close()` (блокирующий сон до границы), `fallback_secs()`, `now()` и `as_timestamp()`
- [x] 2.4 Реализовать валидацию таймфрейма (неизвестный -> `ValueError` с перечнем)
- [x] 2.5 Добавить unit-тесты `tests/unit/scheduler/test_timing.py` с фейковым `clock`: границы для `1h`/`5m`, неделя, месяц, fallback-пауза

## 3. Кэш данных

- [x] 3.1 Создать `src/data/cache.py` с классом `MarketDataCache` (хранит DataFrame на инструмент, ленивая первичная загрузка)
- [x] 3.2 Реализовать определение готовности свечи по границе таймфрейма (через `CandleScheduler`) и исключение незакрытой свечи из данных для анализа
- [x] 3.3 Реализовать инкрементальную дозагрузку только новых баров поверх кэша (через `start_date` = последний известный бар)
- [x] 3.4 Добавить unit-тесты `tests/unit/data/test_cache.py`: первичная загрузка, «бара ещё нет» (без перезагрузки), появление нового бара (дозагрузка), исключение незакрытой свечи

## 4. Порт исполнения

- [x] 4.1 Создать `src/execution/port.py` с абстрактным `ExecutionPort` (метод `execute(decision, instrument)`)
- [x] 4.2 Реализовать `NotifyOnlyExecutionPort` — доставка решения через нотификатор, без ордеров
- [x] 4.3 Добавить unit-тесты `tests/unit/execution/test_port.py`: вызов `execute` для уведомляющего порта доставляет сообщение, ордера не создаются

## 5. Оркестратор-сценарий

- [x] 5.1 Создать `src/bot/trading_bot.py` с классом `TradingBot` (DI через конструктор: instruments, notifier, strategy_map, data_cache, timeline, execution, strategy_factory)
- [x] 5.2 Реализовать метод `run()` (запуск, валидация привязок до цикла, обработка Ctrl+C)
- [x] 5.3 Реализовать метод `_loop()`: цикл «тик = закрытие свечи» через `timeline.wait_until_candle_close()`, перехват исключений тика с уведомлением трейдера и fallback-паузой
- [x] 5.4 Реализовать методы анализа по инструменту и по стратегии (выбор стратегий по типу акции/фьючерса, get из кэша, compute -> decide)
- [x] 5.5 Реализовать защиту от дублей: хранить последнее состояние «(инструмент, стратегия) -> (SignalType, цена)», уведомлять только при изменении
- [x] 5.6 Реализовать направление решения в единый выходной путь `execution.execute(...)` после фронта сигнала (уведомление доставляет `NotifyOnlyExecutionPort`; нотификатор оркестратора используется для алертов об ошибках)
- [x] 5.7 Добавить unit-тесты `tests/unit/bot/test_trading_bot.py` с фейковыми зависимостями: валидация fail-fast, диспетчеризация фьючерса/акции, дедуб-сигналов, ошибка стратегии не блокирует, ошибка тика уведомляет трейдера

## 6. Точка входа и миграция

- [x] 6.1 Переписать `run.py`: сборка зависимостей (select_instruments, get_notifier, MarketDataCache, CandleScheduler, NotifyOnlyExecutionPort) и вызов `TradingBot(...).run()`
- [x] 6.2 Удалить `src/scheduler/runner.py` и `tests/unit/scheduler/test_runner.py` (уточнить с владельцем необходимость временного реэкспорта `run_bot` перед удалением)
- [x] 6.3 Прогнать `pytest`: снэпшот-тесты стратегий и остальные unit-тесты (api/data/notifier/strategies) остаются зелёными; убедиться, что упали только удалённые тесты runner.py
- [x] 6.4 Прогнать `ruff check` и при необходимости `ruff format` по изменённым файлам

## 7. Периодическое сердцебиение

- [x] 7.1 Добавить параметр `heartbeat_every_ticks` в конструктор `TradingBot` (по умолчанию 60) и счётчики выполненных тиков и ошибок за период
- [x] 7.2 Реализовать в `_loop()`/`_tick()` отправку сердцебиения через нотификатор каждые `heartbeat_every_ticks` тиков (со счётчиком тиков и числом ошибок за период)
- [x] 7.3 Учесть ошибки тика (из `_report_error`) в счётчике ошибок за период
- [x] 7.4 Прокинуть `heartbeat_every_ticks` в `run.py`
- [x] 7.5 Добавить unit-тесты сердцебиения: отправка по расписанию (каждые N тиков), не отправляется на каждом тике, число ошибок в сообщении
