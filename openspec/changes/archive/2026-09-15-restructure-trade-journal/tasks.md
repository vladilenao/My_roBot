## 1. Конфигурация

- [x] 1.1 Добавить `positions_file` в маппинг `_CONFIG_KEYS["trading"]` и `_EXPECTED_TYPES` в `src/config_loader.py`; реализовать выводное имя по умолчанию из `journal_file` (`<имя>_positions.csv`)
- [x] 1.2 Добавить константу `POSITIONS_FILE` в `src/config.py` (рядом с `JOURNAL_FILE`); опционально отразить в `default.toml`
- [x] 1.3 Тесты: `positions_file` задан / не задан (выводное имя) / пустая строка → `ConfigError` (`tests/unit/test_trading_config.py`)

## 2. Слой журнала (`src/trade_journal`)

- [x] 2.1 Определить колонки ленты и перечень типов операций `ЗАЯВКА/ВХОД/ДОБОР/ТЕЙК/ВЫХОД/СНИМОК/ОТМЕНА` (единый enum/константа)
- [x] 2.2 Переписать `JournalEvent`, `COLUMNS`, `COLUMNS_RU` под новую схему (design, решение 3); перевести `format_dt`/`parse_dt` на МСК
- [x] 2.3 Реализовать журнал-ленту: append-only запись с новым заголовком, корректный `next_id`, чтение с отбрасыванием битых строк и проверкой схемы заголовка
- [x] 2.4 Реализовать карточки позиций: инкрементальное обновление в памяти на append + перезапись `positions.csv` снапшотом; полный пересчёт реплеем при старте
- [x] 2.5 Переписать `replay_events`: баланс из последнего СНИМОК (иначе стартовый депозит), агрегация ВХОД+ДОБОР (средняя цена), остаток после ТЕЙК/ВЫХОД, восстановление заявок ЗАЯВКА по `order_id`, накопление PnL/комиссии
- [x] 2.6 Unit-тесты журнала (`tests/unit/trade_journal/`): новая схема записи/чтения, битые строки не прерывают реплей, корректная карточка по сценарию вход→добор→тейк→выход

## 3. Брокерский слой (`src/broker/journal_broker.py`)

- [x] 3.1 Пробросить отображение тикер → короткое имя (short name) в `JournalBroker`; использовать его в строках журнала и во всех пользовательских сообщениях событий (заявка/вход/закрытие/отклонение), внутренние ключи оставить на тикере
- [x] 3.2 Перевести построение записей на новые типы операций: ЗАЯВКА+ВХОД/ДОБОР вместо NEW/FILLED, СНИМОК вместо CLEARING, ОТМЕНА для отменённых/истёкших; исполнение связывается с заявкой по `order_id`
- [x] 3.3 Закрывающие операции (ТЕЙК/ВЫХОД): расчёт `pnl_часть` и комиссии, накопление итогов в карточке позиции
- [x] 3.4 Тесты брокера (`tests/unit/broker/`): полная линейка вход→добор→тейк→выход, реплей заявок/позиций/баланса, снимок при клиринге

## 4. Исполнение и адаптер

- [x] 4.1 `src/broker/exec_adapter.py` / `src/execution/port.py`: передача короткого имени контракта в сигнал и событийные сообщения (правило «NG-10.26 на виду, тикер внутри»)
- [x] 4.2 Тесты адаптера/порта (`tests/unit/execution/`, `tests/unit/portfolio/` при необходимости): сообщения и журнал используют короткое имя

## 5. Интеграция и миграция

- [x] 5.1 Миграция при старте (`create_journal_broker`): при наличии легаси `trade_journal.csv` — копия в `trade_journal.csv.bak`, создание чистых `journal.csv`/`positions.csv`
- [x] 5.2 Обновить `tests/integration/test_trading_simulation.py` под новую модель; прогнать весь набор pytest
- [x] 5.3 Обновить `CHANGELOG.md` (секция `## Unreleased`) тезисами о новой структуре журнала