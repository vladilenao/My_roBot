# Tasks: Мульти-таймфреймный ритм

## 1. Конфигурация и контракты

- [x] 1.1 `src/strategies/contracts.py`: поле `timeframe` в `Assignment` (тип str, без дефолта на уровне контракта — каскад разворачивает config)
- [x] 1.2 `src/config_loader.py`: легализовать ключ `timeframe` в таблице тикера и `tf` в инлайн-записи; значения валидируются по `TIMEFRAMES` (иначе `ConfigError` с перечнем); прочие неизвестные ключи по-прежнему отклоняются
- [x] 1.3 `src/config.py`: каскад таймфрейма при построении `Assignment` (`tf` инлайна → `timeframe` тикера → глобальный `TIMEFRAME`); экспорт множества активных ТФ (например, `ACTIVE_TIMEFRAMES`)

## 2. Планировщик (timing)

- [x] 2.1 `src/scheduler/timing.py`: координатор активных сеток поверх `CandleScheduler` (математика одной сетки не меняется): следующий тик = минимум `next_candle_close` по активным ТФ; на пробуждении — множество ТФ с пройденной границей
- [x] 2.2 Ожидание публикации бара per ТФ: общий poll-цикл с `tick.timeout_secs`, per-ТФ проверка готовности; ТФ без свежего бара по таймауту пропускает тик, не блокируя остальные
- [x] 2.3 Fallback-пауза: `min(sleep_seconds, период наименьшего активного ТФ)`, запись об ограничении в лог

## 3. Данные и контекст

- [x] 3.1 `src/data/cache.py`: ключи (инструмент, ТФ); `frame_for(instrument, tf)`; свежесть закрытого бара per паре; убрать конструкторское `timeline.timeframe` (ТФ — параметр вызова, загрузчик его уже принимает)
- [x] 3.2 `src/market_context/context_cache.py`: `get_context(instrument, tf)`; ключ (инструмент, ТФ); контекст вычисляется на кадре своего ТФ

## 4. Оркестратор и нотификации

- [x] 4.1 `TradingBot`: кэш стратегий по ключу (имя, ТФ) — дубли на разных ТФ получают разные инстансы
- [x] 4.2 `TradingBot`: маршрутизация тика — обрабатываются привязки ТФ с закрывшейся свечой; `decide(ta, timeframe=assignment.timeframe)`; ТФ задачи пробрасывается в `_emit`
- [x] 4.3 `ExecutionPort`/`NotifyOnlyExecutionPort`: kwarg `timeframe`; `DecisionFormatter.format`/`AbstractNotifier.notify_decision`: параметр `timeframe` (блок `(ТФ)` из него; конструктор форматтера ТФ теряет); `get_notifier()` обновлён
- [x] 4.4 `run.py`: сборка с активными ТФ из конфига (координатор сеток, кэш, контекст, бот)

## 5. Тесты

- [x] 5.1 Unit-тесты конфига: каскад ТФ (все три уровня), валидация `tf`/`timeframe` (недопустимое значение → ConfigError), старые конфиги валидны
- [x] 5.2 Unit-тесты координатора сеток: границы 15m+1h (:15/:30/:45 — только 15m; :00 — оба), неделимая смесь 15m+1d, одиночный ТФ = прежнее поведение, fallback-clamp
- [x] 5.3 Unit-тесты кэша и контекста: независимость пар (инструмент, ТФ), свежесть per ТФ
- [x] 5.4 Unit-тесты бота: на тике работают только ТФ с закрывшейся свечой; дубли стратегии на разных ТФ — разные инстансы; ТФ задачи доезжает до порта/нотификатора
- [x] 5.5 Обновить существующие unit-тесты под новые сигнатуры (cache/context/bot/notifier/port)
- [x] 5.6 Полный прогон: `pytest tests/unit` + `pytest tests/snapshot`

## 6. Выпуск 2.1.0

- [x] 6.1 `src/__init__.py`: `__version__ = "2.1.0"` (MINOR — обратно совместимо)
- [x] 6.2 `CHANGELOG.md`: секция 2.1.0 с примером каскада ТФ (`tf` → `timeframe` → `[robot].timeframe`)
- [x] 6.3 `ARCHITECTURE.md`: ритм «тик = закрытая свеча любого активного ТФ», строки модулей `scheduler`/`data`/`market_context` — пары (инструмент, ТФ), пример привязки с `tf`
