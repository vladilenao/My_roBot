# Дельта: signals

## MODIFIED Requirements

### Requirement: Формат сообщения с instrument_label
Формирование сообщений выполняет класс `DecisionFormatter` из пакета `src/notifier`: метод форматирования ДОЛЖЕН принимать решение (`Decision`) и опциональный `instrument_label`, добавляя префикс `[instrument_label] `. Модульная функция `format_decision` и функция `make_decision` НЕ ДОЛЖНЫ существовать.

#### Scenario: С label
- **WHEN** метод форматирования вызван с `instrument_label="NG (Природный газ) — NG-9.26"`
- **THEN** сообщение начинается с `[NG (Природный газ) — NG-9.26]`

#### Scenario: Без label
- **WHEN** метод форматирования вызван с пустым `instrument_label`
- **THEN** префикс `[...]` не добавляется

### Requirement: Формат цены
Округление цены в сообщениях выполняет `DecisionFormatter`: цена в сообщениях ПОКУПКА/ПРОДАЖА округляется до 3 знаков после запятой.

#### Scenario: Округление цены
- **WHEN** цена решения равна 3.14159
- **THEN** в сообщении `3.142`

### Requirement: Эмодзи в сообщениях
Эмодзи принадлежат `DecisionFormatter`: ПОКУПКА — "🚀 ПОКУПАТЬ!"; ПРОДАЖА — "📉 ПРОДАВАТЬ!"; УДЕРЖАНИЕ — "😴 Отдыхаем, сигналов нет.".

#### Scenario: Эмодзи присутствуют
- **WHEN** форматтер получает любое из трёх решений
- **THEN** соответствующее эмодзи присутствует в тексте
