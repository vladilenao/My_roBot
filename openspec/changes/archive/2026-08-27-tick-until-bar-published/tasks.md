## 1. Планировщик времени (src/scheduler/timing.py)

- [x] 1.1 Добавить в `CandleScheduler` метод `wait_until_bar_published(bar_ready, poll_secs=1.0, timeout_secs=65.0)` (см. design D1): ждёт до границы закрытия текущей свечи, затем в цикле вызывает `bar_ready()`, при `True` завершает; иначе спит `poll_secs` и повторяет; останавливается по истечении `timeout_secs`, измеряемого через инжектированный `clock`/`now()`.
- [x] 1.2 Обеспечить тестируемость паузы: выделить внутренний шаг сна так, чтобы unit-тесты могли подставлять `poll_secs=0` или заменять `time.sleep` (согласовано с инжекцией фиктивного времени).
- [x] 1.3 Добавить unit-тесты в `tests/unit/` (см. src/scheduler): бар доступен сразу → мгновенный возврат; бар появляется после нескольких попыток → дожидается; бар не появляется → завершение по таймауту.

## 2. Признак готовности бара в кэше (src/data/cache.py)

- [x] 2.1 Добавить метод `MarketDataCache` для проверки/ожидания появления свежего закрытого бара текущей закрытой минуты: вычисляет ожидаемую метку `expected = current_candle_start(now) - period` и сообщает, виден ли закрытый бар с `datetime >= expected` (design D2).
- [x] 2.2 Встроить повторную дозагрузку: `bar_ready`/цикл ожидания вызывает `refresh_if_new_candle()` перед проверкой, чтобы подтягивать свежий бар при публикации с задержкой (design D2).
- [x] 2.3 Добавить unit-тесты в `tests/unit/data/test_cache.py`: бар ещё не в данных → признак ложь и дозагрузка повторяется; бар появился после повторной попытки → признак истина; по таймауту отдаются имеющиеся данные (сценарии «Свежий бар появляется после повторных попыток» и «Таймаут ожидания бара»).

## 3. Оркестрация (src/bot/trading_bot.py)

- [x] 3.1 В `_loop()`/`_tick()` подключить ожидание бара перед обработкой: заменить прямой `wait_until_candle_close()` на `wait_until_bar_published(bar_ready, ...)`, где `bar_ready` собирает проверку кэша (design D2, D5) и передаётся `poll_secs`/`timeout_secs` из конфига.
- [x] 3.2 Убедиться, что при истечении таймаута тик продолжается обычным путём: `refresh_if_new_candle()` + `_process(...)` с имеющимися данными (сценарий «Обработка по таймауту»), без пропуска тика и без сбоя цикла.
- [x] 3.3 Добавить unit-тесты в `tests/unit/bot/test_trading_bot.py` на сценарии из orchestration: тик удерживается до появления свежего бара; обработка происходит по таймауту с имеющимися данными (с инъекцией фиктивного времени и фейкового кэша).

## 4. Конфигурация (src/config.py)

- [x] 4.1 Добавить `TICK_POLL_SECS` (пауза между попытками дозагрузки, ~1 с) и `TICK_TIMEOUT_SECS` (таймаут ожидания бара, ~65 с) с дефолтами; связать с передачей в планировщик/оркестратор (design D4).
- [x] 4.2 Зафиксировать связь конфига с новыми параметрами без поломки существующих вызовов (дефолты сохраняют старое поведение при отсутствии новых значений).

## 5. Проверка и документация

- [x] 5.1 Прогнать `pytest tests/unit` — все модульные тесты проходят (включая новые и существующие).
- [x] 5.2 Прогнать `ruff check` по изменённым файлам.
- [x] 5.3 Прогнать `openspec validate --specs` — дельты и основная спека валидны.
- [ ] 5.4 Пилотный запуск на одном инструменте (NG) в проде для подтверждения: `bar_time` свежий, дубли/пропуски отсутствуют.
