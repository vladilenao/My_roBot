## 1. Схема v9: таблица коротких имён

- [x] 1.1 В `src/trade_journal/schema.py` поднять `SCHEMA_VERSION = 9` и добавить `MIGRATE_V8_TO_V9_SQL`: `CREATE TABLE instrument_names (ticker TEXT PRIMARY KEY, short_name TEXT NOT NULL, updated_at TEXT NOT NULL)`
- [x] 1.2 Добавить ветку перехода v8→v9 в цепочки `initialize_schema` и в обратные проверки версии, чтобы базы v1…v8 доводились до v9
- [x] 1.3 Обновить `docs/database.md`: версия схемы 9 и описание `instrument_names`

## 2. Карта имён в журнале

- [x] 2.1 В `src/trade_journal/storage.py::set_names` рядом с передачей карты экспортёру сохранять карту в `instrument_names` идемпотентным upsert (один `executemany` в той же транзакции)
- [x] 2.2 Добавить чтение сохранённой карты (`instrument_names()`) для автономных инструментов; карта возвращается как `Mapping[str, str]`
- [x] 2.3 Убедиться, что `set_names` не создаёт лишних ревизий экспорта и не меняет формат `trade_event.csv` / `trade_summary.csv`

## 3. Документ `docs/trade-management/signal-pipeline.md`

- [x] 3.1 `flowchart` порядка шагов тика: защита по младшему ТФ → управление позициями → сбор кандидатов → сортировка по приоритету → допуск, с ветками отказа и обозначенной границей транзакции записи
- [x] 3.2 `sequenceDiagram` пути одного сигнала до подтверждённого входа или отказа, с ветками отказа и явным показом двух счётчиков занятости (подтверждённые позиции против строк `reservations`)
- [x] 3.3 `stateDiagram-v2` фаз сделки и времени жизни резервации, включая освобождение при переходе в терминальную фазу
- [x] 3.4 Таблица ответственности объектов: владелец состояния, кто имеет право его менять, кто не имеет права; зафиксировать, что SQLite — единственный источник истины
- [x] 3.5 Ссылки на существующие документы (`docs/trade-management/profiles.md`, `storage-and-audit.md`, `docs/database.md`) без дублирования их содержимого
- [x] 3.6 Связать документ из `docs/trade-management/README.md`

## 4. Инструмент `tools/show_state.py`

- [x] 4.1 Открытие базы по URI `file:<path>?mode=ro` без `Storage` и без `initialize_schema`; понятная диагностика, если `-shm` отсутствует или база на старой версии схемы
- [x] 4.2 Один снимок: все чтения в одной транзакции `BEGIN DEFERRED`, на выводе — время снимка
- [x] 4.3 Блок «счёт и лимиты»: баланс, эквити, `budget_base = min(balance, equity)`, загрузка лимитов из `src.config.RISK_LIMITS` в процентах
- [x] 4.4 Блок «сделки в работе»: фаза, сторона, объём, средняя, подтверждённый стоп, риск до стопа
- [x] 4.5 Блок «активные резервы»: сделка, риск, маржа, время создания, возраст; метка `СИРОТА` при `ACTIVE` и терминальной фазе сделки
- [x] 4.6 Блок «последние отказы биржи» с читаемой причиной из `events` (отказы на допуске в базе не сохраняются — см. design, ограничение 6)
- [x] 4.7 Толерантный парсер времени: ISO без смещения, ISO с `+00:00`, `YYYY-MM-DD HH:MM:SS`; приведение к МСК, нераспознанный формат — явная метка
- [x] 4.8 Короткие имена из `instrument_names`; при отсутствии карты — сообщение без вывода сырых тикеров
- [x] 4.9 Только стандартная библиотека, текстовые таблицы фиксированной ширины

## 5. Тесты

- [x] 5.1 Unit в `tests/unit/trade_journal/`: `set_names` пишет карту, повторный вызов не дублирует строки, чтение возвращает ту же карту
- [x] 5.2 Интеграционный тест миграции v8→v9 в `tests/integration/`: таблица появляется, существующие строки не меняются, `user_version` = 9
- [x] 5.3 Тест read-only режима: инструмент не создаёт схему на базе предыдущей версии и не изменяет файл базы (сравнение контрольной суммы до/после)
- [x] 5.4 Unit `tests/unit/test_show_state.py`: сборка отчёта по фикстуре — счёт, загрузка лимитов, сделка в работе, сиротская резервация, отказы биржи; каждый формат времени; отсутствие карты имён
- [x] 5.5 Проверить отсутствие сырых тикеров в выводе отчёта тестом (правило `openspec/config.yaml`)
- [x] 5.6 Прогнать `.venv/bin/python -m pytest -q` и `.venv/bin/python -m ruff check .` (допустимы прежние ошибки в `tools/` и `openspec/_check_sync.py`)

## 6. Проверка на живых данных

- [x] 6.1 Снять резервную копию `data/trades.sqlite3` через `sqlite3 .backup` (файл в WAL, простое копирование не подходит)
- [x] 6.2 Перезапустить робот: убедиться, что миграция v9 прошла и `instrument_names` заполнена
- [x] 6.3 Сверить счёт и число активных резервов из отчёта с `trade_summary.csv`
