## 1. Specs

- [x] 1.1 MODIFIED requirement «Размер позиции по итоговому риску» в дельте
      `specs/risk-management/spec.md` (геометрия тейка, кап через резку qty,
      агрегат по открытым) — проверено `openspec validate`
- [x] 1.2 MODIFIED requirement «Карточка позиции» в дельте
      `specs/trade-journal/spec.md` (журнал пишет фактическое значение риска)

## 2. Design

- [x] 2.1 `design.md`: формула фактического риска от геометрии полного тейка,
      порядок «кап → затем агрегат», открытый вопрос по трейлинг-профилям

## 3. Реализация

- [x] 3.1 `src/portfolio/manager.py`: `risk_rub` пересчитывается от
      `(полный_тейк − стоп)/step × step_cost × qty`; кап бюджета урезает qty
- [x] 3.2 `src/portfolio/manager.py`: агрегат Σ риск открытых + риск новой ≤
       бюджета; при превышении вход отклоняется (пример NG 2000 + Si 400)
- [x] 3.3 `src/trade_journal/journal_broker.py`: в журнал пишется фактическое
      значение `risk_rub`, а не бюджет
- [x] 3.4 тесты `portfolio` (кап, агрегат, урезание qty) + snapshot журнала
      (ожидаемые значения риска в CSV)

## 4. Миграция

- [x] 4.1 Обновить существующие CSV-снапшоты журнала под фактический риск;
      старые файлы сохраняются отдельно при первом экспорте
