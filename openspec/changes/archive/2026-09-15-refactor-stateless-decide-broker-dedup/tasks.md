# Задачи: stateless decide + дедупликация входа в брокере

Статус-сетка: группу 1 выполняем только при переносе торгового слоя с `feature/deal-note` (выбор пользователя). Группы 2–3 независимы по правкам и идут после Основной группы 1.

## 1. Перенос торгового слоя с feature/deal-note

- [x] 1.1 Перенести исходники `src/broker/` (JournalBroker, порт, exec_adapter), `src/trade_journal/`, `src/portfolio/` (PositionManager, ContractMeta, Signal, Account) с ветки `feature/deal-note` и соответствующие unit-тесты
- [x] 1.2 Собрать интеграцию `run.py` (построение брокерского исполнения, загрузка контрактов, `_load_contracts_metadata`) как на ветке deal-note; убедиться, что реестр/конфигурация стартуют
- [x] 1.3 Удалить осиротевшие пустые пакеты `src/broker/`, `src/trade_journal/`, `src/portfolio/` вместе со следами `__pycache__`, если они остаются лишними после переноса
- [x] 1.4 Прогреть тесты перенесённого слоя (портфель, брокер, журнал сделок) до зелёного

## 2. Stateless-рефакторинг MA Cloud RSI MACD

- [x] 2.1 Ввести неизменяемую структуру `MaCloudState` (pending_long/pending_short/long_contracts/short_contracts) в `src/strategies/ma_cloud_rsi_macd_strategy.py`
- [x] 2.2 Перенести логику `decide()`/`_check_exit`/`_check_scale_in` в чистый шаг `_step(state, bar_idx, row) -> (state, Decision)`; сохранить порядок (пурж окна ENTRY_WINDOW → регистрация голосов → вход → выход → добор) и гвард прогрева «bar_idx < 2 → HOLD без мутаций»
- [x] 2.3 Реализовать `decide(ta, timeframe=None)` как однопроходный реплей через `_step`; инстанс НЕ хранит состояния между вызовами; вернуть решение последнего бара
- [x] 2.4 Реализовать `expected_events(ta)` через тот же однопроходный реплей (сбор не-HOLD событий в `EVENT_COLUMNS`), убрав O(n²) вызовов на префиксах и свежий runner
- [x] 2.5 Добавить внутренний шов `_decide_from_state(ta, state, timeframe)` для проверки решения из известного состояния; переписать ~13 unit-тестов, инжектирующих приватное состояние, на этот шов
- [x] 2.6 Проверить, что все вызывающие `decide()` в проде получают полную/префиксную историю (grep по `trading_bot.py` и `expected_events`)
- [x] 2.7 Добавить тесты идемпотентности: `decide(ta)` дважды на одном инстансе → идентичный Decision; «два профиля одного ТФ» видят одинаковое решение; `expected_events` на инстансе с предысторией вызовов даёт тот же результат
- [x] 2.8 Прогнать snapshot-раннер БЕЗ перегенерации эталонов (эталоны должны остаться зелёными) и полный набор unit-тестов

## 3. Дедупликация входа в брокере

- [x] 3.1 В брокерском слое создания ордера ввести ограниченную карту дедупликации: ключ (инструмент, таймфрейм, `bar_time`) для решений с `action="entry"`
- [x] 3.2 Повторный вход с тем же ключом: ордер НЕ создаётся, в журнале/логе фиксируется причина `duplicate`; консольное уведомление не подавляется
- [x] 3.3 Прунинг карты по устаревшим `bar_time` (карта не растёт); выходы и доборы НЕ дедуплицируются; следующий бар создаёт ордер
- [x] 3.4 Добавить `TestBrokerEntryDedup`: один вход за бар (два одинаковых входящих сигнала → один ордер), разные бары — новые ордера, выходы/доборы не блокируются
- [x] 3.5 Контрактный e2e-сценарий: SI 5m с двумя привязками `ma_cloud_rsi_macd` (`raw` + `triple_screen`), обе прошли фильтры → в журнале ровно один ордер на вход, второй с причиной `duplicate`

## 4. Конфигурация и сценарий сравнения привязок

- [x] 4.1 Зафиксировать в `default.toml` сценарий сравнения: `SI` — две привязки `ma_cloud_rsi_macd` на 5m с профилями `raw` и `triple_screen` (пользовательский выбор «один инструмент, один ТФ, разные фильтры»)
- [x] 4.2 Решить судьбу второй SI-привязки на 15m/1h согласно предыдущим версиям конфигурации и при необходимости обновить комментарии в `default.toml`

## 5. Финальная проверка

- [x] 5.1 Полный прогон `pytest` (unit + snapshot): финальный счёт без регрессий
- [x] 5.2 Сборка/прогон бота с новой конфигурацией: обе привязки SI 5m показывают одинаковое решение, различие — только результат фильтра; дубликат входа фиксируется причиной `duplicate`