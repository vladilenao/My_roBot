## MODIFIED Requirements

### Requirement: Изолированный пакет стратегии
Каждая стратегия ДОЛЖНА располагаться в собственном файле по единому правилу именования `src/strategies/<имя>_strategy.py`, где `<имя>` — идентификатор стратегии (`NAME`). Общая инфраструктура пакета `src/strategies/` (конфигурация и Builder — `base_strategy.py`, реестр — `registry.py`, агрегация — `signals.py`, перечень имён — `names.py`, контракты — `contracts.py`, индикаторы — `indicators/`) НЕ ДОЛЖНА размещаться в файлах с суффиксом `_strategy`, чтобы имя файла однозначно указывало на роль модуля. Идентификатор стратегии (`NAME`, `StrategyName`, привязки в `config.py`) НЕ зависит от имени файла: переименование файла не меняет зарегистрированное имя. Стратегия ДОЛЖНА владеть своей математикой, правилом событий и окном; глобальные параметры расчёта стратегий в `src.config` НЕ ДОЛЖНЫ существовать. Добавление новой стратегии НЕ ДОЛЖНО требовать правки движка, unit-тестов других стратегий, snapshot-раннера или скрипта скачивания.

#### Scenario: Файл стратегии по правилу именования
- **WHEN** создаётся новая стратегия с идентификатором `NAME = "my_strategy"`
- **THEN** её файл называется `src/strategies/my_strategy_strategy.py`

#### Scenario: Имя файла отделено от идентификатора
- **WHEN** файл стратегии получает суффикс `_strategy`, а её `NAME` не меняется
- **THEN** стратегия остаётся доступной в реестре под прежним именем без правки привязок, конфигурации и snapshot-раннера

#### Scenario: Инфраструктура не использует суффикс _strategy
- **WHEN** в `src/strategies/` добавляется инфраструктурный модуль
- **THEN** его имя не заканчивается на `_strategy` (например, общая конфигурация живёт в `base_strategy.py`)

#### Scenario: Добавление новой стратегии
- **WHEN** создан новый файл `src/strategies/<имя>_strategy.py` с регистрацией в реестре и привязка добавлена в соответствующий словарь (`SHARE_STRATEGIES` или `FUTURE_STRATEGIES`)
- **THEN** стратегия работает в боте, покрывается единым snapshot-раннером и доступна скрипту скачивания без правки их кода