## 1. Переработка цикла дозагрузки

- [x] 1.1 В `refresh_if_new_candle` собирать кандидатов — кадры ТФ с данными (не `None`, не пустые), не отслеженные для текущей границы (или все при `force`), и сортировать их по возрастанию последнего закрытого бара (`datetime.min`, если закрытых баров нет); пустые/`None` кадры по-прежнему помечать `_observed = boundary`
- [x] 1.2 В ветке `_throttled` вернуть управление без записи в `_observed` (троттлированный кадр остаётся кандидатом на следующее окно)
- [x] 1.3 В ветке `RESOURCE_EXHAUSTED` выставлять `_retry_after`, логировать и завершать вызов вместо `continue`
- [x] 1.4 Обновить docstring `refresh_if_new_candle`: честное распределение окон между кадрами ТФ, приоритет самому отстающему

## 2. Unit-тесты

- [x] 2.1 Тест: 3 пары на 1h с поздно опубликованными барами — за число окон ≤ числу пар каждый кадр обновляется и гейт открывается
- [x] 2.2 Тест: при `force` в закрытом окне `_observed` кадров не меняется и API не дёргается; после открытия окна дозагружается самый отстающий кадр
- [x] 2.3 Тест: отстающий кадр (данные ещё не обновились) перезапрашивается в цикле, а свежий кадр не трогается

## 3. Проверка

- [x] 3.1 `.venv/bin/python -m pytest -q` — весь набор зелёный (база: 912 passed, 6 skipped)
- [x] 3.2 `.venv/bin/python -m ruff check src tests run.py` — без новых замечаний