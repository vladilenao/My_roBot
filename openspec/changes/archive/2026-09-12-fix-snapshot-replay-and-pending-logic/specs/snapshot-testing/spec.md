## MODIFIED Requirements

### Requirement: Прогон фикстуры через боевой модуль
Тест ДОЛЖЕН прогонять фикстуру через боевые методы стратегии из реестра: события эталона вычисляет `expected_events()` выбранной по имени стратегии, агрегация повторяет её логику (скользящее окно `STRATEGY_WINDOW` над сигнальными колонками, правило событий стратегии). Прямые вызовы удалённых модулей (`tech_analyze` как единой точки расчёта) в тестах НЕ ДОЛЖНЫ использоваться. Для стратегий с внутренним состоянием (pending-сигналы, окно входа) реплей ДОЛЖЕН подавать `decide()` накопительные фреймы — от начала прогрева до текущей свечи — чтобы семантика повторяла боевой цикл; прогон по коротким фиксированным чанкам, искажающий отсчёт абсолютной позиции, ЗАПРЕЩЁН.

#### Scenario: Воспроизведение оконной агрегации
- **WHEN** фикстура содержит N ≥ STRATEGY_WINDOW строк
- **THEN** `expected_events()` стратегии возвращает ровно те события, что даёт скользящая агрегация последних STRATEGY_WINDOW значений сигнальных колонок

#### Scenario: Реплей для стратегий с состоянием накопителен
- **WHEN** стратегия обладает внутренним состоянием (например, `ma_cloud_rsi_macd` с окном входа `ENTRY_WINDOW`)
- **THEN** `expected_events()` вызывает `decide()` на накопительном фрейме для каждой свечи, и события совпадают с прогоном полной истории за один вызов

#### Scenario: Идентичные события в snapshot и в бою
- **WHEN** одна и та же стратегия прогоняется через snapshot-раннер (накопительный реплей) и через боевой цикл с той же фикстурой
- **THEN** множества событий BUY/SELL совпадают