## 1. Исправление pending-логики стратегии

- [x] 1.1 Перевести `_pending_long`/`_pending_short` со списков int на карты «имя индикатора → бар первого сигнала» (`dict[str, int]`) в `src/strategies/ma_cloud_rsi_macd_strategy.py`.
- [x] 1.2 Фиксировать сигнал индикатора только если его имени ещё нет в карте направления — повторные сигналы того же индикатора не засчитываются вторым/третьим подтверждением.
- [x] 1.3 Прунить карты по абсолютному `bar_idx`: запись удаляется при `bar_idx - first_sig_bar >= ENTRY_WINDOW`, после чего индикатор может «перезапуститься».
- [x] 1.4 Проверять вход как наличие трёх различных индикаторов в направлении (Long — бычие, Short — медвежьи); после входа очищать карту направления.
- [x] 1.5 Обновить docstring/комментарии: `decide(ta)` получает накопительную историю, `bar_idx = len(ta) - 1` абсолютен.

## 2. expected_events и накопительный реплей

- [x] 2.1 Добавить метод `expected_events(ta)` в `MaCloudRsiMacdStrategy`: покадровый накопительный прогон `decide(ta.iloc[:i+1])` от конца прогрева индикаторов (закрывает отсутствие обязательного по контракту метода).
- [x] 2.2 Привести `_ma_cloud_rsi_macd_expected_events` в `tests/snapshot/helper.py` к накопительному вызову `decide(ta.iloc[:i+1])`, убрав чанки `iloc[max(0, i-2):i+1]` (рекомендовано делегирование методу стратегии).
- [x] 2.3 Убедиться, что `tools/download_snapshot_data.py` (`strategy.expected_events`) и `tests/snapshot/helper.py` дают идентичные эталоны; устранить расхождения.

## 3. Unit-тесты

- [x] 3.1 Обновить существующие тесты pending на новую структуру (в т.ч. pruning — `tests/unit/strategies/test_ma_cloud_rsi_macd.py:156`).
- [x] 3.2 Тест: повторный сигнал того же индикатора не считается вторым подтверждением → `HOLD` (spec: «Повторный сигнал того же индикатора не считается вторым»).
- [x] 3.3 Тест: три разных индикатора в окне дают `BUY`/`SELL`, состояние очищается (spec: «Третий индикатор подтверждает Long/Short»).
- [x] 3.4 Тест: истечение окна → сброс, индикатор может сработать заново (spec: «Окно сигнала истекло»).
- [x] 3.5 Тест: `expected_events()` накопительным реплеем даёт события, идентичные событиям из полной истории (spec snapshot-testing: «Реплей для стратегий с состоянием накопителен»).

## 4. Эталоны данных

- [x] 4.1 Перегенерировать `ma_cloud_rsi_macd_expected_signals.csv` для NG_1h, BR_1h, SBER_1d через `pytest --update-snapshots`; отревьюить git diff каждого файла.
- [x] 4.2 Если ни один кейс не даёт входов — сгенерировать дополнительный торговый кейс (другой инструмент/таймфрейм) через `tools/download_snapshot_data.py` и добавить эталон; нулевые эталоны зафиксировать как принятое решение (design D4). — Условие не сработало: SBER_1d даёт SELL-вход, эталон непустой; пустые NG_1h/BR_1h зафиксированы как принятые (D4).
- [x] 4.3 `tests/snapshot/test_strategies.py` и `tests/snapshot/test_analysis.py` проходят на обновлённых данных.

## 5. Визуализатор

- [x] 5.1 Адаптировать `tools/visualize_signals.py` под исправленные события: выбор «чистого» входа — осознанный диагностический режим, а не маскировка.
- [x] 5.2 При отсутствии сигнала индикатора в окне панель честно показывает его отсутствие (design D5).
- [x] 5.3 Перегенерировать SVGs; smoke-тесты визуализации и проверка картинок подписей проходят.

## 6. Верификация

- [x] 6.1 Полный прогон `pytest` (unit + snapshot).
- [x] 6.2 `ruff check src/ tests/ run.py` — чисто; `ruff format` применён.
- [x] 6.3 `openspec validate fix-snapshot-replay-and-pending-logic` — проходит; `openspec status` — все артефакты done.