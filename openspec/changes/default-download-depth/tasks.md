# Задачи: дефолтная глубина скачивания = 300

## 1. Код скрипта

- [x] 1.1 В `tools/download_snapshot_data.py`: удалить `calculate_depth()` и `WARMUP_RESERVE`, задать глубину запроса = `HARD_LIMIT` (300); импорт `SIGNAL_WINDOW` сохранить для эталона
- [x] 1.2 Обновить докстринг модуля (упоминание формулы/глубины) и вывод «Глубина запроса»

## 2. Верификация и фиксация

- [ ] 2.1 Перекачать оба кейса штатным скриптом (NGU6 → NG_1h, BRU6 → BR_1h): запрошено 300, получено ≤300; эталоны регенерированы
- [ ] 2.2 Полный `pytest` — все тесты зелёные на обновлённых фикстурах
- [ ] 2.3 Закоммитить код + фикстуры (pre-commit хук прогонит pytest)
