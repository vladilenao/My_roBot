# Кэш рыночного контекста

## Purpose

Ленивый кэш рыночного контекста (TrendResult + SRLevels) по инструментам. Экономит вычисления: контекст пересчитывается только при появлении новой закрытой свечи, а не при каждом вызове. На одном тике для инструмента с N стратегиями контекст вычисляется 1 раз, а не N раз.

## Requirements

### Requirement: Ленивое вычисление контекста
Кэш ДОЛЖЕН вычислять MarketContext только при первом запросе для пары (инструмент, таймфрейм) или при обнаружении новой закрытой свечи её таймфрейма. Повторные запросы на том же тике ДОЛЖНЫ возвращать кэшированный результат без пересчёта. Контексты разных таймфреймов одного инструмента ДОЛЖНЫ вычисляться и храниться независимо.

#### Scenario: Первый запрос
- **WHEN** для пары (инструмент, таймфрейм) ещё не вычислялся контекст
- **THEN** выполняется полный расчёт (trend + S/R) и результат сохраняется в кэш

#### Scenario: Повторный запрос на том же тике
- **WHEN** для пары (инструмент, таймфрейм) контекст уже вычислен и свеча не изменилась
- **THEN** расчёт не выполняется, возвращается кэшированный результат

#### Scenario: Новая свеча
- **WHEN** последняя свеча в DataFrame пары (инструмент, таймфрейм) отличается от той, по которой был вычислен контекст
- **THEN** контекст пересчитывается и кэш обновляется

#### Scenario: Контексты разных ТФ не смешиваются
- **WHEN** для одного инструмента запрошены контексты `15m` и `1h`
- **THEN** каждый вычисляется на кадре своего таймфрейма и хранится отдельно

### Requirement: Инвалидация по свече
Кэш ДОЛЖЕН определять необходимость пересчёта по временной метке последней свечи в DataFrame (сравнение `datetime` последней строки). Сравнение ДОЛЖНО выполняться в tz-naive UTC виде, согласованном с MarketDataCache.

#### Scenario: Свеча не изменилась
- **WHEN** `datetime` последней свечи DataFrame совпадает с сохранённой меткой
- **THEN** пересчёт не выполняется

#### Scenario: Свеча обновилась
- **WHEN** `datetime` последней свечи DataFrame больше сохранённой метки
- **THEN** выполняется пересчёт и метка обновляется

### Requirement: Привязка к MarketDataCache
Кэш ДОЛЖЕН получать MarketDataCache через конструктор и использовать его для получения DataFrame свечей пары (инструмент, таймфрейм). Кэш НЕ ДОЛЖЕН владеть данными или модифицировать MarketDataCache.

#### Scenario: Зависимость от MarketDataCache
- **WHEN** MarketDataCache не содержит данных для пары (инструмент, таймфрейм)
- **THEN** кэш возвращает пустой результат (не выбрасывает ошибку)
