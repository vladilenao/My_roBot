schema: spec-driven

context: |
  Python-бот для торговых сигналов (Tinkoff Invest API): src/ — рабочий код, tests/ — pytest.
  Организация дерева тестов (обязательное):
  - tests/unit/ — быстрые изолированные unit-тесты (без файлов данных); tests/snapshot/ — тяжёлые data-driven snapshot-тесты; файлы тестов в корне tests/ запрещены (кроме conftest.py)
  - Стратегия = файл tests/snapshot/test_<стратегия>.py
  - Кейс = папка tests/snapshot/data/<ИНСТРУМЕНТ>_<ТАЙМФРЕЙМ>/ с ровно одним candles.csv
  - Эталон = tests/snapshot/data/<кейс>/<стратегия>_expected_signals.csv (без префиксов, точное имя)
  - Свечи кейса общие для всех стратегий; уровень стратегии в путях данных запрещён

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-operation guidance (optional)
# Add advisory guidance for how apply and archive work should be conducted.
# This is separate from artifact rules above.
# Example:
#   operations:
#     apply:
#       guidance:
#         - Keep test summaries concise
#     archive:
#       guidance:
#         - Summarize the archive outcome before finishing
